# Laravel 12.61.1 — WSC2026 app + MySQL 8.4

```bash
docker compose up --build
```

Open **http://localhost**. On start it generates an app key, waits for MySQL 8.4,
runs migrations, then serves. Pinned: PHP 8.3 / Composer 2.9.5, laravel/framework 12.61.1, MySQL 8.4.
